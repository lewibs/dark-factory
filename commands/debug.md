---
description: "Debug a non-obvious bug. Creates a worktree, runs debugger-agent, and opens a PR with the fix."
---

Follow the instructions in `agents/dark-factory/agents/debug-command-agent.md` exactly.
